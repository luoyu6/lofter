function deleteSpace(str) {
	var newstr = str.replace(/\s+/g, ""); //去掉空格		
	return newstr;
}
// 定义格式化日期的函数,time参数单位是秒
function formartTime(time) {
	var m = Math.floor(time / 60);
	var s = Math.floor(time % 60);
	return(m < 10 ? "0" + m : m) + ":" + (s < 10 ? "0" + s : s);
}